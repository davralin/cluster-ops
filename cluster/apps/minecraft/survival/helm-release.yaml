---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: survival
  namespace: minecraft
spec:
  interval: 5m
  chart:
    spec:
      chart: minecraft
      version: 4.2.0
      sourceRef:
        kind: HelmRepository
        name: itzg-charts
        namespace: flux-system
      interval: 5m
  targetNamespace: minecraft
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    minecraftServer:
      eula: true
      type: SPIGOT
      overrideServerProperties: true
      whitelist: X
      ops: X
      max_players: 3
      mode: survival
      server_name: Survival
      seed: 421112feedmekittens
      pvp: true
      tz: "{TIMEZONE}"
    podAnnotations:
      mc-router.itzg.me/externalServerName: "survival.${SECRET_PRIVATE_DOMAIN}"
      backup.velero.io/backup-volumes: backupdir
    persistence:
      dataDir:
        enabled: true
    mcbackup:
      enabled: true
      pruneBackupsDays: 3
      persistence:
        backupDir:
          enabled: true