---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rcloneconfig
  namespace: minio
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 32Mi