---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: minio-local-usb
  namespace: minio
spec:
  storageClassName: manual
  capacity:
    storage: 4Ti
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: /var/media/usb/minio
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-local-usb
  namespace: minio
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Ti
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: daily-sync-to-usb
  namespace: minio
spec:
  schedule: "03 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            feature.node.kubernetes.io/custom-usb-backupdrive: "true"
          restartPolicy: Never
          containers:
            - name: daily-sync-to-usb
              image: b2un0/rsync:1.0.1
              command: ['/usr/bin/rsync']
              args: ["--archive", "--delete", "--progress", "/media/", "/srv/"]
              volumeMounts:
                - mountPath: /media
                  name: minio-local
                  readOnly: true
                - mountPath: /srv
                  name: minio-local-usb
                  readOnly: false
          volumes:
            - name: minio-local
              persistentVolumeClaim:
                claimName: minio-local
            - name: minio-local-usb
              persistentVolumeClaim:
                claimName: minio-local-usb
