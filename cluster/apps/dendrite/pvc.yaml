---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dendrite-db
  namespace: dendrite
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi