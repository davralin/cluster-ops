---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: lidarr
  name: lidarr
  namespace: media
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
