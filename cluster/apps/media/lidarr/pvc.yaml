---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: lidarr
  name: lidarr
  namespace: media
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
