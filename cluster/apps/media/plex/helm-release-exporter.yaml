---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plex-exporter
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 5m
  targetNamespace: media
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    fullnameOverride: plex-exporter
    podSecurityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
      fsGroupChangePolicy: "OnRootMismatch"
    image:
      repository: ghcr.io/jsclayton/prometheus-plex-exporter
      tag: latest@sha256:918fbb6f0fe1b86a2c28b2e50cf0fc50e5f61e75dd73eba9d201900bee2dc6d2
    service:
      main:
        ports:
          http:
            port: 9000
    env:
      TZ: "${TIMEZONE}"
      PLEX_SERVER: "http://plex.media.svc.cluster.local:32400"
      PLEX_TOKEN: "${SECRET_PLEX_TOKEN}"
    serviceMonitor:
      main:
        enabled: "${MONITORING_PROMETHEUS}"