---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: plex
  name: plex
  namespace: media
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi