---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: sonarr
  name: sonarr
  namespace: media
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
