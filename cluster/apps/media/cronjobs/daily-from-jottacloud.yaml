---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: daily-from-jottacloud
  namespace: media
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: daily-from-jottacloud
              image: rclone/rclone:1.63.1
              args:
                - sync
                - --ignore-case
                - --check-first
                - --delete-before
                - --verbose
                - --exclude
                - video/**
                - jottacloud-crypt:/media
                - /media/
              volumeMounts:
                - mountPath: /media
                  name: media
                  readOnly: false
                - mountPath: /config
                  name: rcloneconfig
                  readOnly: false
          volumes:
            - name: media
              persistentVolumeClaim:
                claimName: media
            - name: rcloneconfig
              persistentVolumeClaim:
                claimName: rcloneconfig
            - name: rclone-config
              secret:
                secretName: rclone-config
                items:
                  - key: rclone.conf
                    path: rclone.conf
