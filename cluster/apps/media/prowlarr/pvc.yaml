---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: prowlarr
  name: prowlarr
  namespace: media
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
