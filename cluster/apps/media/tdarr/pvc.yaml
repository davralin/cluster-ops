---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr
  namespace: media
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi