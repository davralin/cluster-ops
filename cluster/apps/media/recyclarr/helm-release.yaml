---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: recyclarr
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 5m
  targetNamespace: media
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    fullnameOverride: recyclarr
    podSecurityContext:
      runAsUser: 0
      runAsGroup: 0
      fsGroup: 0
      fsGroupChangePolicy: "OnRootMismatch"
    image:
      repository: ghcr.io/recyclarr/recyclarr
      tag: 5.1.0@sha256:85a0d9231530316fa95a535cdda7c08bfff530c307858fc9d070fb6a611a1ed8
    service:
      main:
        enabled: false
    env:
      CRON_SCHEDULE: "* * * * *"
      TZ: "${TIMEZONE}"
    persistence:
      config:
        enabled: true
        existingClaim: recyclarr
    podAnnotations:
      backup.velero.io/backup-volumes: config
    resources:
      requests:
        cpu: 5m
        memory: 36M
      limits:
        memory: 256M
