---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: auto-m4b
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 5m
  targetNamespace: media
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    fullnameOverride: auto-m4b
    # Init-system runs as root
    #podSecurityContext:
      #runAsUser: 1000
      #runAsGroup: 1000
      #fsGroup: 1000
      #fsGroupChangePolicy: "OnRootMismatch"
    image:
      repository: seanap/auto-m4b
      tag: latest@sha256:d1a87791bd34cdb424bc1d55a8b43a025528e83f1a0e1e681c0e3458d6b5657d
    service:
      main:
        enabled: false
    env:
      TZ: "${TIMEZONE}"
      PUID: 1000
      PGID: 1000
      UMASK: 000
    persistence:
      backup:
        enabled: true
        type: emptyDir
        mountPath: /temp/backup
      temp:
        enabled: true
        existingClaim: media
        subPath: unsorted/audiobooks/auto-m4b
        mountPath: /temp
    resources:
      requests:
        cpu: 50m
        memory: 200Mi
      limits:
        memory: 400Mi