---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: readarr
  name: readarr
  namespace: media
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
