---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: readarr
  name: readarr
  namespace: media
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
