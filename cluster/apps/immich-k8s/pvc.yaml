---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich
  namespace: immich-k8s
spec:
  storageClassName: "${STORAGE_READWRITEMANY}"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi