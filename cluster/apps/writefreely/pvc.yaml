---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: writefreely
  name: writefreely
  namespace: writefreely
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi