---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - namespace.yaml
secretGenerator:
- name: docker-cloudflare-config
  namespace: ddns
  files:
  - config-yaml
generatorOptions:
  disableNameSuffixHash: true