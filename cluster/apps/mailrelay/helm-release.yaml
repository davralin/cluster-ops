---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &name exim
  namespace: mailrelay
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 2.3.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 5m
  targetNamespace: mailrelay
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  valuesFrom:
    - targetPath: controllers.main.containers.main.env.SMARTHOST
      kind: Secret
      name: mail-smarthost
      valuesKey: smarthost
    - targetPath: controllers.main.containers.main.env.SMTP_USERNAME
      kind: Secret
      name: mail-smarthost
      valuesKey: username
    - targetPath: controllers.main.containers.main.env.SMTP_PASSWORD
      kind: Secret
      name: mail-smarthost
      valuesKey: password
  values:
    fullnameOverride: *name
    defaultPodOptions:
      securityContext:
        runAsUser: 100
        runAsGroup: 101
        fsGroup: 100
        fsGroupChangePolicy: "OnRootMismatch"
    controllers:
      main:
        type: daemonset
        containers:
          main:
            image:
              repository: devture/exim-relay
              tag: 4.96-r1-0
            env:
              HOSTNAME: "smtp.${SECRET_DEFAULT_DOMAIN}"
            resources:
              requests:
                cpu: 50m
                memory: 80Mi
              limits:
                cpu: 100m
                memory: 128Mi
    service:
      main:
        type: LoadBalancer
        ports:
          http:
            enabled: false
            primary: false
          mail:
            primary: true
            port: 25
            targetPort: 8025
            protocol: TCP