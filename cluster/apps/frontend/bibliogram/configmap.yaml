---
apiVersion: v1
kind: ConfigMap
metadata:
  name: bibliogram-config
  namespace: frontend
data:
  config.js: |
    module.exports = {
        website_origin: "https://i.${SECRET_PUBLIC_DOMAIN}"
    }