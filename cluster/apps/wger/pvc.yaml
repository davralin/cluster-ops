---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wger-postgresql
  namespace: wger
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi