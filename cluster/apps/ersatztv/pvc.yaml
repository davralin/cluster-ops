---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ersatztv
  namespace: ersatztv
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi