---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: frigate
  name: frigate
  namespace: homeassistant
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi