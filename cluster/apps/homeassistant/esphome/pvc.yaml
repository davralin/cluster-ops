---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: esphome
  name: esphome
  namespace: homeassistant
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi