---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mqtt
  name: mqtt
  namespace: homeassistant
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi