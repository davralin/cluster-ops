---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rcloneconfig
  namespace: nextcloud
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 32Mi