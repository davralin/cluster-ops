---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: typesense
  namespace: nextcloud
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
