---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism
  namespace: nextcloud
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi