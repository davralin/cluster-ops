---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: hourly-calendar-sync
  name: hourly-calendar-sync
  namespace: jobs
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi