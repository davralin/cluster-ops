---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: unifi-controller
  name: unifi
  namespace: unifi
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi