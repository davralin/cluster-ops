---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: airprint
  name: airprint
  namespace: airprint
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi