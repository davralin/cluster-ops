---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
  - namespace.yaml
secretGenerator:
- name: velero-s3-creds
  namespace: velero
  files:
  - velero-s3-creds
generatorOptions:
  disableNameSuffixHash: true
