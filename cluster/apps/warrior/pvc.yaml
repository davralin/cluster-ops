---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: warrior
  name: warrior
  namespace: warrior
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi