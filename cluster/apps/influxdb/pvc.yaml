---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: influxdb
  namespace: monitoring
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi