---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mariadb-kanboard
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: mariadb
      version: 11.0.8
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system
      interval: 5m
  targetNamespace: kanboard
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    fullnameOverride: mariadb
    image:
      repository: bitnami/mariadb
      tag: 10.7.4
    auth:
      database: kanboard
      username: kanboard
      existingSecret: kanboard-mariadb
    primary:
      podAnnotations:
        backup.velero.io/backup-volumes: data
      persistence:
        enabled: true
        existingClaim: kanboard-mariadb
    volumePermissions:
      enabled: true
    metrics:
      enabled: true
      image:
        repository: bitnami/mysqld-exporter
        tag: 0.14.0
    volumePermissions:
      image:
        repository: bitnami/bitnami-shell
        tag: 10