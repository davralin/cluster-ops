---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kanboard-mariadb
  namespace: kanboard
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi