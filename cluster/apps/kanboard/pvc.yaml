---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: kanboard
  name: kanboard
  namespace: kanboard
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi