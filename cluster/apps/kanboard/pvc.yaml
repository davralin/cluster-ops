---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kanboard
  namespace: kanboard
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
