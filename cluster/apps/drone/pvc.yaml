---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drone
  namespace: drone
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi