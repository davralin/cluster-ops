---
apiVersion: v1
kind: Service
metadata:
  name: matrix-alertmanager
  namespace: matrix
spec:
  selector:
    app: matrix-alertmanager
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
