---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mx-discord
  name: mx-discord
  namespace: matrix
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi