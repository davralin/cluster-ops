---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database
  namespace: matrix
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi