---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dendrite
  namespace: matrix
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi