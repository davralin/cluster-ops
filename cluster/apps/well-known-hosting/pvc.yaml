---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wellknown
  namespace: wellknown
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi