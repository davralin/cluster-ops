---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wellknown
  namespace: wellknown
  annotations:
    volumeType: local
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
