---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database
  namespace: synapse
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
