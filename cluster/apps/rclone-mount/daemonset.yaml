---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: rclone-mount
  namespace: rclone
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: rclone-mount
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rclone-mount
    spec:
      initContainers:
      - name: media-mount-config
        image: busybox:1.36.1
        command:
          - cp
        args:
          - -v
          - /in/rclone.conf
          - /out/rclone.conf
        volumeMounts:
        - name: config-data
          mountPath: /in
        - name: config
          mountPath: /out
      terminationGracePeriodSeconds: 120
      containers:
      - name: media-mount
        image: rclone/rclone:1.64.2
        args:
          - mount
          - "jottacloud-crypt:"
          - /var/media/rclone
          - --config=/etc/rclone/rclone.conf
          - --allow-non-empty
          - --allow-other
          - --read-only
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh","-c","fusermount -uz /var/media/rclone"]
        securityContext:
          privileged: true
          capabilities:
            add:
              - SYS_ADMIN
        volumeMounts:
        - name: config
          mountPath: /etc/rclone
        - name: data
          mountPath: /var/lib/rclone
        - name: media
          mountPath: /var/media/rclone
          mountPropagation: Bidirectional
      volumes:
      - name: config
        emptyDir: {}
      - name: config-data
        secret:
          secretName: rclone-config
      - name: media
        hostPath:
          path: /var/media/rclone
      - name: data
        hostPath:
          path: /var/lib/rclone/
