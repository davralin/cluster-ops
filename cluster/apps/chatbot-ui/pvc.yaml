---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chatbotui
  namespace: chatbotui
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi