---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smokeping
  namespace: smokeping
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
