---
- name: Set sshd_config
  template:
    src: sshd_config.j2
    dest: /etc/ssh/sshd_config
    group: root
    owner: root
    force: yes
    mode: 0644
    validate: /usr/sbin/sshd -t -f %s
  become: yes
  notify: Restart sshd
