---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actualbudget
  namespace: actualbudget
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
