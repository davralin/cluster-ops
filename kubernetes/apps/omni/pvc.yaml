---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omni
  namespace: omni
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi