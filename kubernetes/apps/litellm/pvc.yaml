---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: litellm
  namespace: litellm
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
