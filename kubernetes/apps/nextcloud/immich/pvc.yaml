---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich
  namespace: nextcloud
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
