---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencode
  namespace: opencode
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi