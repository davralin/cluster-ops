---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omniroute
  namespace: omniroute
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi