---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: awx-deployment
  namespace: awx
spec:
  interval: 30m
  chart:
    spec:
      chart: raw
      version: v0.3.2
      sourceRef:
        kind: HelmRepository
        name: dysnix-charts
        namespace: flux-system
      interval: 30m
  driftDetection:
    mode: enabled
  targetNamespace: awx
  install:
    createNamespace: true
    remediation:
      retries: 10
  upgrade:
    remediation:
      retries: 10
  dependsOn:
    - name: awx-operator
      namespace: flux-system
    - name: awx-postgres-v18
      namespace: flux-system
  values:
    resources:
      - apiVersion: awx.ansible.com/v1beta1
        kind: AWX
        metadata:
          name: awx
          namespace: awx
        spec:
          ee_images:
            - name: personal-awx-ee
              image: ghcr.io/davralin/awx-ee:latest@sha256:3dde6c22a855dd98c34f7263517d4180029099e0dd5752d65d2a08d8a6725f49
          extra_settings:
            - setting: TOWER_URL_BASE
              value: '"https://awx.${SECRET_PRIVATE_DOMAIN}"'
          hostname: "awx.${SECRET_PRIVATE_DOMAIN}"
          ingress_annotations: |
            haproxy.org/allow-list: "${HAPROXY_WHITELIST}"
            haproxy.org/ssl-redirect-port: "443"
            haproxy.org/response-set-header: |
              Strict-Transport-Security "max-age=31536000"
              X-Frame-Options "DENY"
              X-Content-Type-Options "nosniff"
              Referrer-Policy "strict-origin-when-cross-origin"
          ingress_type: ingress
          ingress_tls_secret: "${SECRET_PRIVATE_DOMAIN_CERT}"
          ipv6_disabled: true
          postgres_configuration_secret: awx-secrets
          replicas: 1
