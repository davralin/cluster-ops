---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium
  namespace: technitium
spec:
  storageClassName: "${STORAGE_READWRITEONCE}"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi