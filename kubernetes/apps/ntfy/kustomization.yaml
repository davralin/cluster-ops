---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
  - namespace.yaml
  - pvc.yaml
configMapGenerator:
  - name: ntfy-config
    namespace: ntfy
    files:
      - config.yaml=./config.yaml
generatorOptions:
  disableNameSuffixHash: true