---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name tuppr
  namespace: flux-system
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: tuppr
  driftDetection:
    mode: enabled
  targetNamespace: talos-system
  install:
    createNamespace: true
    crds: CreateReplace
    remediation:
      retries: 10
  upgrade:
    remediation:
      retries: 10
    crds: CreateReplace
  values:
    fullnameOverride: *name
    monitoring:
      dashboards:
        enabled: "${MONITORING_PROMETHEUS}"
      prometheusRule:
        enabled: "${MONITORING_PROMETHEUS}"
      serviceMonitor:
        enabled: "${MONITORING_PROMETHEUS}"
    notification:
      enabled: true
      secretName: tuppr-gotify
      secretKey: url
    securityContext:
      seccompProfile:
        type: RuntimeDefault
